
import { Alumni_Sans, Barlow, Exo, IBM_Plex_Mono, Lilita_One, Luckiest_Guy, Pacifico } from "next/font/google";

export const pacifico = Pacifico({
    subsets: ["latin"],
    weight: ["400"], // optional weights
    display: "swap", // optional
});
export const mono = IBM_Plex_Mono({
    subsets: ["latin"],
    weight: ["400"], // optional weights
    display: "swap", // optional
});
export const alumni = Barlow({
    subsets: ["latin"],
    weight: ["400"], // optional weights
    display: "swap", // optional
});
export const lilita = Lilita_One({
    subsets: ["latin"],
    weight: ["400"], // optional weights
    display: "swap", // optional
});
export const guy = Luckiest_Guy({
    subsets: ["latin"],
    weight: ["400"], // optional weights
    display: "swap", // optional
});
export const exo = Exo({
    subsets: ["latin"],
    weight: ["400"], // optional weights
    display: "swap", // optional
});
